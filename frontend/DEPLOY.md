# 🚀 Деплой на GitHub Pages

## Быстрая инструкция

### 1. Включите GitHub Pages в настройках репозитория

1. Зайдите в ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** выберите: **GitHub Actions**

### 2. Запушьте код

```bash
git add .
git commit -m "Setup GitHub Pages deployment"
git push
```

### 3. Проверьте деплой

1. Зайдите в **Actions** в вашем репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. Сайт будет доступен по адресу:
   ```
   https://ваш-username.github.io/hackathon-urban-tech/
   ```
   (или `https://ваш-username.github.io/название-репозитория/`)

## Обновление данных

После обновления данных в ML pipeline:

```bash
# Скопируйте данные
cp ml/output/*.json frontend/data/
cp ml/output/*.csv frontend/data/

# Запушьте изменения
git add frontend/data/
git commit -m "Update data"
git push
```

GitHub Actions автоматически задеплоит обновленную версию.

## Если что-то не работает

1. Проверьте, что workflow запустился: **Actions** → **Deploy to GitHub Pages**
2. Проверьте логи workflow на наличие ошибок
3. Убедитесь, что в Settings → Pages выбрано "GitHub Actions" как источник

