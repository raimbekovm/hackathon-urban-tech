# 🚀 Быстрый старт: Использование ваших данных

## 📥 Шаг 1: Экспорт данных в формате YOLO

### Если используете LabelImg, Roboflow, CVAT или другой инструмент:

1. **Экспортируйте в формате YOLO** (не COCO, не Pascal VOC!)
2. **Структура должна быть:**
   ```
   ваши_данные/
   ├── train/
   │   ├── images/
   │   └── labels/
   ├── val/
   │   ├── images/
   │   └── labels/
   └── test/ (опционально)
       ├── images/
       └── labels/
   ```

3. **Классы должны соответствовать RDD2022:**
   - `0` = longitudinal_crack (продольная трещина)
   - `1` = transverse_crack (поперечная трещина)
   - `2` = alligator_crack (сетка трещин)
   - `3` = pothole (яма/кочка)
   - `4` = other_damage (другие дефекты)

## ✅ Шаг 2: Проверка данных

```bash
cd ml
python check_dataset.py путь/к/вашим/данным
```

Скрипт проверит:
- ✅ Структуру папок
- ✅ Соответствие изображений и аннотаций
- ✅ Формат аннотаций
- ✅ Распределение классов

## 📂 Шаг 3: Размещение данных в проекте

```bash
# Скопируйте данные в папку ml/data/
cp -r ваши_данные ml/data/bishkek_annotated
```

## ⚙️ Шаг 4: Создание конфигурации

Создайте файл `ml/data/bishkek.yaml`:

```yaml
path: data/bishkek_annotated
train: train/images
val: val/images
test: test/images

nc: 5
names:
  0: longitudinal_crack
  1: transverse_crack
  2: alligator_crack
  3: pothole
  4: other_damage
```

## 🎓 Шаг 5: Обучение модели

```python
from ultralytics import YOLO

# Загрузите предобученную модель
model = YOLO('yolov8n.pt')  # или yolov8s.pt для лучшего качества

# Обучите на ваших данных
model.train(
    data='data/bishkek.yaml',
    epochs=50,
    imgsz=640,
    batch=16,
    device=0  # 0 для GPU, cpu для CPU
)

# Модель сохранится в runs/train/weights/best.pt
```

## 🔄 Шаг 6: Использование обученной модели

```python
from ultralytics import YOLO

# Загрузите вашу обученную модель
model = YOLO('runs/train/weights/best.pt')

# Обработайте изображения
results = model('path/to/image.jpg')

# Или используйте в process_real_data.py
# Просто укажите путь к модели:
python process_real_data.py --model runs/train/weights/best.pt
```

## 📚 Подробная документация

См. `DATA_FORMAT_GUIDE.md` для детальной информации о формате данных.

## ❓ Частые вопросы

**Q: Можно ли объединить мои данные с RDD2022?**
A: Да! Просто скопируйте изображения и аннотации из обоих датасетов в одну папку train/images и train/labels.

**Q: Сколько данных нужно?**
A: Минимум 100 изображений на класс для хорошего качества. Чем больше - тем лучше!

**Q: Нужен ли GPU?**
A: Рекомендуется, но можно обучать и на CPU (будет медленнее).

